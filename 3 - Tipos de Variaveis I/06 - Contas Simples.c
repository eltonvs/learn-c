#include <stdio.h>
int main(void){
  int a, b, soma, sub, mult, divi;
  printf("Digite dois números: ");
  scanf("%d %d", %a, %b);
  soma = a+b;
  sub = a-b;
  mult = a*b;
  divi = a/b;
  printf("\nO resultado de %d + %d eh %d\n", a, b, soma);
  printf("O resultado de %d - %d eh %d\n", a, b, sub);
  printf("O resultado de %d x %d eh %d\n", a, b, mult);
  printf("O resultado de %d / %d eh %d\n", a, b, divi);
  return 0;
}
